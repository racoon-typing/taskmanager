const COLORS = ['black', 'yellow', 'blue', 'green', 'pink'];

const FilterType = {
  ALL: 'all',
  OVERDUE: 'overdue',
  TODAY: 'today',
  FAVORITES: 'favorites',
  REPEATING: 'repeating',
  ARCHIVE: 'archive',
};

const SortType = {
  DEFAULT: 'default',
  DATE_DOWN: 'date_down',
  DATE_UP: 'date_up'
};

export {COLORS, FilterType, SortType};
